// Import: Dependencies
import React from "react";

// Import: Elements
import { Container } from "./ReportContainer.elements";

// Component: ReportContainer
export default function ReportContainer({ children }) {
  return <Container>{children}</Container>;
}
